import styled from "styled-components/macro";

export const Wrapper = styled.div`
  flex: 1;
  position: relative;
  overflow: hidden;

  .map {
    position: absolute;
    width: 100%;
    height: 100%;
  }
`;
