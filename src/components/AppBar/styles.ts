import styled from "styled-components";

export const Wrapper = styled.header`
  position: relative;
  z-index: 1002;

  > div {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
`;
